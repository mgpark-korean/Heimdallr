import React from 'react';
import ReactDOM from 'react-dom';
import {SiteMapBoard} from "./siteMapBoard/SiteMapBoard";

ReactDOM.render(
  <React.StrictMode>
    <SiteMapBoard />
  </React.StrictMode>,
  document.getElementById('root')
);
